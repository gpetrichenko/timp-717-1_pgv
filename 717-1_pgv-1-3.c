#include <stdio.h>
#include <math.h>
int main()
{
    int x;
    int y;
    int num3;
    scanf( "%d", &x );
    scanf( "%d", &y );
    num3 = pow(x,y);
    printf("%d", num3);
    getchar();
    return 0;
}
